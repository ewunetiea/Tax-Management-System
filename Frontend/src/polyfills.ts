/***************************************************************************************************
 * Zone JS is required by default for Angular itself.
 */
// import 'zone.js';

/***************************************************************************************************
 * APPLICATION IMPORTS
 */

// (window as any).global = window;
// Ensure Node-style globals exist in the browser for libraries like sockjs-client
(window as any).global = window as any;
